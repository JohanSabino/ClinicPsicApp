[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "php artisan migrate --force && (php artisan db:seed --force || echo 'Seeders failed, continuing...') && php artisan serve --host=0.0.0.0 --port=$PORT"